# 後端 FastAPI 的位址(瀏覽器會直接打這個位址,必須是「瀏覽器能連到」的位址)
#
# 本機開發(在伺服器同一台開 localhost:3000):
#   NEXT_PUBLIC_API_BASE=http://localhost:8000
#
# 用伺服器公有 IP / 域名存取(例如瀏覽器開 http://18.142.237.50:3000):
#   請改成伺服器對外可連到的後端位址,例如:
#   NEXT_PUBLIC_API_BASE=http://18.142.237.50:8000
NEXT_PUBLIC_API_BASE=http://localhost:8000

# (選用)允許從哪些來源存取 dev server 的 /_next/* 資源,消除 Next.js 跨來源警告。
# 以逗號分隔,填入你用來開前端的 IP / 域名(不含 http:// 與 port)。
# 例如:ALLOWED_DEV_ORIGINS=18.142.237.50
ALLOWED_DEV_ORIGINS=
